# TZ: SHTIRLITZ Landing Interactive Demo Finish

Date: 2026-07-07  
Audience: Claude Code  
Repo: `C:\Users\fedor\Obsidian\Brain\Projects\Stirlitz (IBP)\repo-landing`

## Context

This is not a greenfield landing page. The repo already contains an undeployed interactive demo:

- `app/page.tsx`
- `components/tour/TypeSelect.tsx`
- `components/tour/CheckForm.tsx`
- `components/tour/PipelineRun.tsx`
- `components/tour/DossierReveal.tsx`
- `components/tour/TourContext.tsx`
- `app/translations.ts`
- `app/globals.css`

The live site at `shtirlitz-landing.vercel.app` is still the older static landing page with Hero/Pain/Pipeline/Comparison/Pricing. The local repo is newer and should be finished, validated, then deployed.

## Product

Brand spelling:

- Russian: `ШТИРЛИЦ`
- English: `SHTIRLITZ`
- Do not use `Shtirlits`.
- Do not try to reconcile the live domain transliteration mismatch: `shtirletzsled.ru` stays as-is.

Language:

- Bilingual.
- Russian is default.
- English uses the existing client-side language toggle.
- Keep copy parity in `app/translations.ts`.
- No URL split and no i18n routing.

Primary positioning:

- Use the neutral hero idea: `Автоматическая проверка кандидатов. За 5 минут.`
- English: `Automated candidate screening. In 5 minutes.`
- Do not lead with competitor naming.
- Mention `Буратино` only in a restored comparison section below the demo.

Target buyers:

- B2B HR teams.
- Corporate security teams.
- Recruitment agencies.
- Do not reposition toward banks, journalists, lawyers, or consumer users.

## Hard Constraints

- Demo data must be 100% fictional.
- No network calls from the marketing demo.
- No real auth.
- No real OSINT lookups.
- No data submission to the backend.
- Do not modify the real Flask login at `https://shtirletzsled.ru/login`.
- The demo login is decorative and must always succeed.
- Keep the demo fast enough for a landing page.
- Respect `prefers-reduced-motion`.
- Keep all new visuals inside the existing design token system.

## Design System

Use the existing palette in `app/globals.css`:

- Background: `#0A0A0A`
- Card: `#1A1A1A`
- Elevated panel: `#111111`
- Primary text: `#E8E0D4`
- Muted/dim/ghost text: `#E8E0D4` at 67%, 40%, 20%
- Gold accent: `#C4955A`
- Gold hover: `#D4A56A`
- Terminal green: `#4ADE80`, only for pipeline logs/checkmarks
- Border: `#E8E0D4` at 6%
- Border strong: `#E8E0D4` at 10%
- Risk clean: `#EBDBBC`
- Risk medium: `#D4A27F`
- Risk high: `#CC785C`
- Risk critical: `#CC5247`
- No data: `#666663`

Typography:

- Bebas Neue for brand/large display headlines.
- JetBrains Mono for labels, logs, IDs, metadata, small technical text.
- Mulish/body font for readable prose.

Visual language:

- Dark OSINT/intelligence interface.
- Thin borders.
- Hairline dividers.
- Terminal/dossier UI.
- Registry cards, pipeline stages, graph-like layouts, risk tiles.
- No stock photography.
- No colorful new palette.
- No decorative blobs/orbs.
- No generic SaaS hero layout.

Reference assets:

- Palette image exists locally at `C:\Users\fedor\technical task\палитра.png`.
- The provided dashboard screenshot is the visual target for the post-login investigation type screen.
- Any future reference images should be placed in a reference folder and tagged by purpose: `dashboard mood`, `dossier layout`, `graph visuals`, `login style`, or `color/type`.
- If bitmap assets are needed, they should be custom generated by Codex and saved under `public/generated/`. Prefer code-native React/CSS/SVG visuals when that matches the interface better.

## Required User Flow

Final landing flow:

1. Nav
2. Intro walkthrough
3. Decorative demo login
4. Investigation type dashboard
5. Check form
6. Animated pipeline
7. Dossier reveal
8. Restored Pain section
9. Restored Comparison section
10. Pricing
11. Testimonials
12. FinalCTA
13. Footer

Current `app/page.tsx` starts at `TypeSelect`. Add the new intro/login before it and restore Pain/Comparison below the tour.

## Intro Walkthrough

Create a new component, for example:

- `components/tour/IntroWalkthrough.tsx`

Purpose:

- First thing visitors see.
- Explain what SHTIRLITZ is and what pain it solves.
- Should feel like a terminal/dossier boot sequence, not a cinematic video and not a map animation.

Duration:

- 4-6 seconds if watched passively.
- Skippable at all times.
- Reduced motion users should see the final state immediately or near-immediately.

Suggested Russian beats:

1. `ШТИРЛИЦ — платформа автоматической проверки кандидатов`
2. `Ручной OSINT занимает часы и требует обученного аналитика`
3. `Система собирает реестры, суды, санкции, соцсети и связи в одно досье`
4. `HR и служба безопасности получают риск-скоринг за 5 минут`

Suggested English beats:

1. `SHTIRLITZ is an automated candidate screening platform`
2. `Manual OSINT takes hours and requires a trained analyst`
3. `The system brings registries, courts, sanctions, social signals, and links into one dossier`
4. `HR and security teams get a risk score in 5 minutes`

Implementation notes:

- Use existing `scanline`, `typeIn`, and `cursor-blink` keyframes if useful.
- Add a Skip button.
- After completion or skip, advance to demo login.
- Do not create a marketing hero separate from the product experience.

## Demo Login

Create a new component, for example:

- `components/tour/DemoLogin.tsx`

Purpose:

- Decorative transition from landing into the mock software.
- It should look like a login/authentication panel inside the product world.
- It must not be a real login and must not call any backend.

Behavior:

- Auto-fill demo credentials.
- Show authenticating progress for about 1.5 seconds.
- Show checkmark/success state.
- Always succeeds.
- No wrong-password path.
- Continue automatically to `TypeSelect`.
- Provide a manual `Continue`/`Skip` path for accessibility.

Suggested Russian copy:

- Title: `ДЕМО-ДОСТУП`
- Login: `demo@shtirlitz.local`
- Password: `••••••••••••`
- Status 1: `Инициализация защищённой сессии`
- Status 2: `Проверка demo-токена`
- Status 3: `Доступ разрешён`

Suggested English copy:

- Title: `DEMO ACCESS`
- Login: `demo@shtirlitz.local`
- Password: `••••••••••••`
- Status 1: `Initializing secure session`
- Status 2: `Verifying demo token`
- Status 3: `Access granted`

## Investigation Type Dashboard

Existing file:

- `components/tour/TypeSelect.tsx`

Keep the two-card screen as the actual software dashboard:

- Type 01: Physical person / individual.
- Type 02: Legal entity / sole proprietor.

Preserve the current visual direction from the provided dashboard screenshot:

- Large `ШТИРЛИЦ` or page heading.
- Two large rectangular cards.
- Thin borders.
- Gold accent line.
- Small mono labels.
- No extra marketing copy crowding this screen.

The cards should still select type and scroll to the form.

## Check Form

Existing file:

- `components/tour/CheckForm.tsx`

Current form is person-oriented. Make it type-aware.

For `person`:

- Required: full name.
- Required: date of birth.
- Optional: INN.
- Required: consent checkbox.
- Keep the 152-FZ consent text.

For `company`:

- Required: INN.
- Optional: company name, prefilled with a fictional example.
- Hide date of birth.
- Replace person consent with lawful-basis/business-check wording.
- Use fictional example data only.

Suggested company defaults:

- Russian company name: `ООО "Вектор Снаб"`
- Russian INN: `7712345678`
- English company name: `Vector Snab LLC`
- English TIN: `7712345678`

Do not make company users enter personal DOB.

## Pipeline Animation

Existing file:

- `components/tour/PipelineRun.tsx`

Current issue:

- Company currently reuses person pipeline stages.

Requirement:

- Pipeline stages must depend on `checkType`.
- Person stages can stay as the current 9-stage pipeline.
- Company stages must use distinct company/INN content.

Person stages:

1. `Идентификация` / `Identity`
2. `Реестры` / `Registries`
3. `Безопасность` / `Security`
4. `Соцсети` / `Social`
5. `Контакты` / `Contacts`
6. `Глубокий анализ` / `Deep analysis`
7. `Поведение` / `Behavior`
8. `Риски` / `Risk`
9. `Отчёт` / `Report`

Company stages:

1. `Идентификация` / `Identification`
   - RU desc: `ЕГРЮЛ, ИНН, статус компании`
   - EN desc: `Company registry, TIN, legal status`
2. `Учредители и директора` / `Founders & Directors`
   - RU desc: `Роли, доли, история руководства`
   - EN desc: `Roles, shares, management history`
3. `Связанные компании` / `Related Companies`
   - RU desc: `Граф соучредителей и руководителей`
   - EN desc: `Co-founder and director graph`
4. `Арбитраж` / `Arbitration`
   - RU desc: `Арбитражные дела и роли в процессах`
   - EN desc: `Arbitration cases and procedural roles`
5. `Банкротство` / `Bankruptcy`
   - RU desc: `Федресурс, признаки несостоятельности`
   - EN desc: `Fedresurs and insolvency signals`
6. `Санкции и РНП` / `Sanctions & Supplier Blacklist`
   - RU desc: `Санкционные списки и реестр недобросовестных поставщиков`
   - EN desc: `Sanctions lists and unreliable supplier registry`
7. `Госконтракты` / `Government Contracts`
   - RU desc: `Контракты, заказчики, исполнение`
   - EN desc: `Contracts, customers, fulfillment`
8. `Финансовые риски` / `Financial Risks`
   - RU desc: `Выручка, долги, признаки риска`
   - EN desc: `Revenue, debts, risk signals`
9. `Отчёт` / `Report`
   - RU desc: `Генерация досье по компании`
   - EN desc: `Company dossier generation`

Keep the animated per-stage logs. The log line should mention the active stage desc.

## Dossier Reveal

Existing file:

- `components/tour/DossierReveal.tsx`

Current issue:

- Company dossier reuses person-oriented outcome labels like `VK profile` and personal identity fields.

Requirement:

- Keep 8 demo tiles for pace.
- Make dossier outcomes type-aware.
- Person outcomes can stay broadly as-is.
- Company outcomes need company-specific tile labels and tile contents.

Company tile labels:

- `Регистрация` / `Registration`
- `Учредители` / `Founders`
- `Директора` / `Directors`
- `Арбитраж` / `Arbitration`
- `Финансы` / `Financials`
- `Санкции и РНП` / `Sanctions & Blacklists`
- `Госконтракты` / `Gov Contracts`
- `Граф связей` / `Connection Graph`

Company clean outcome:

- Risk score: `12`
- Risk label RU: `НИЗКИЙ РИСК`
- Risk label EN: `LOW RISK`
- Summary RU: `Компания активна, критических признаков риска не обнаружено`
- Summary EN: `The company is active, with no critical risk signals found`
- Registration: `Активна с 2019` / `Active since 2019`
- Founders: `2 учредителя` / `2 founders`
- Directors: `Директор подтверждён` / `Director confirmed`
- Arbitration: `0 активных дел` / `0 active cases`
- Financials: `Выручка стабильна` / `Stable revenue`
- Sanctions: `Совпадений нет` / `No matches`
- Gov contracts: `3 исполненных контракта` / `3 fulfilled contracts`
- Graph: `Связи без красных флагов` / `No red flags in links`

Company medium-risk outcome:

- Risk score: `58`
- Risk label RU: `СРЕДНИЙ РИСК`
- Risk label EN: `MODERATE RISK`
- Summary RU: `Есть признаки повышенной проверки: арбитраж, смена директора, связанные компании`
- Summary EN: `Enhanced review recommended: arbitration, director change, related companies`
- Registration: `Активна, изменения 2025` / `Active, changes in 2025`
- Founders: `1 массовый учредитель` / `1 high-volume founder`
- Directors: `Смена директора` / `Director changed`
- Arbitration: `4 дела за 24 месяца` / `4 cases in 24 months`
- Financials: `Снижение выручки` / `Revenue decline`
- Sanctions: `Санкций нет, РНП чисто` / `No sanctions, blacklist clear`
- Gov contracts: `1 контракт с просрочкой` / `1 delayed contract`
- Graph: `9 связанных юрлиц` / `9 related entities`

The identity/source bar should show a company subject when `checkType === 'company'`, not a person's name.

## Pain Section

Restore a Pain section below the tour. Recover old copy from the previous landing if available in git history or live page source; otherwise implement concise copy using this positioning:

- Manual candidate checks take hours.
- They require a trained analyst.
- Data is fragmented across registries, courts, sanctions, social platforms, and open web.
- HR and security teams miss risks because the process is slow and inconsistent.
- SHTIRLITZ turns that into a repeatable 5-minute workflow with a risk score and dossier.

Keep this section visually restrained and consistent with the product UI.

## Comparison Section

Restore a comparison-vs-`Буратино` section below Pain.

Rules:

- Do not mention the competitor in the hero.
- It is acceptable to mention `Буратино` here.
- Keep tone factual, not childish.
- Compare workflow, speed, source coverage, dossier quality, repeatability, and B2B suitability.

Suggested columns:

- `Ручной / Буратино`
- `ШТИРЛИЦ`

Suggested points:

- Hours/manual analyst vs 5-15 minutes automated pipeline.
- Fragmented results vs unified dossier.
- Hard to repeat consistently vs repeatable risk scoring.
- Narrow/source-specific checks vs registry + courts + sanctions + social + graph.
- Requires expertise vs usable by HR/security operator.

## Implementation Targets

Likely changes:

- `app/page.tsx`
  - Insert `IntroWalkthrough` and `DemoLogin` before `TypeSelect`.
  - Restore `Pain` and `Comparison` before `Pricing`.
- `app/translations.ts`
  - Add intro/login copy.
  - Split pipeline stages by type.
  - Split dossier labels/outcomes by type.
  - Add Pain/Comparison copy.
- `components/tour/TourContext.tsx`
  - Store `checkType`, `subject`, optional `inn`, and flow state if needed.
  - Avoid person-only assumptions.
- `components/tour/IntroWalkthrough.tsx`
  - New.
- `components/tour/DemoLogin.tsx`
  - New.
- `components/tour/CheckForm.tsx`
  - Make fields and validation type-aware.
- `components/tour/PipelineRun.tsx`
  - Choose stages based on `checkType`.
- `components/tour/DossierReveal.tsx`
  - Choose tile labels and outcomes based on `checkType`.
- `components/Pain.tsx`
  - Restore or recreate.
- `components/Comparison.tsx`
  - Restore or recreate.

Keep component scope tight. Do not refactor the entire landing.

## Technical Stack

Existing stack must stay:

- Next.js 16.2.1
- React 19
- TypeScript 5
- Tailwind v4
- Framer Motion 12
- Client-side custom i18n
- Single-page anchor-scroll landing
- Vercel deploy on push to `main`

Validation commands:

- `npm run lint`
- `npm run build`

## Accessibility And Responsiveness

- Mobile and desktop must both work.
- No text overflow inside cards/buttons.
- Intro and login must be skippable.
- Respect reduced motion.
- Keyboard users must be able to advance through intro/login and use both investigation cards.
- Do not trap focus.
- CTA links must remain real links where appropriate.

## Non-Goals

- Do not build real authentication.
- Do not connect the marketing demo to the Flask app.
- Do not add backend routes.
- Do not add real OSINT APIs.
- Do not add pricing/payment logic.
- Do not expand the demo dossier to the full production 12-tile dossier unless explicitly requested later.
- Do not replace the whole design system.

## Acceptance Criteria

The work is done when:

- First visit shows a skippable intro walkthrough before the dashboard.
- Intro explains what SHTIRLITZ is, the manual OSINT pain, and the 5-minute B2B screening value.
- Demo login auto-fills, authenticates visually, always succeeds, and leads into the dashboard.
- The investigation type dashboard still matches the provided dark/gold screenshot direction.
- Person card starts a person-specific form, pipeline, and dossier.
- Company card starts a company-specific INN flow, company pipeline, and company dossier.
- Company flow contains no person-only artifacts like DOB, VK profile, personal name, or personal consent wording.
- Dossier remains fictional and clearly labeled as demo data.
- Pain and Comparison sections are present below the tour.
- The hero/lead copy is neutral and competitor-free.
- `Буратино` appears only in the comparison section if used.
- RU/EN toggle works for all newly added sections and states.
- `npm run lint` passes.
- `npm run build` passes.
- After merge/push, Vercel serves the new interactive demo instead of the old static page.

